package com.vikash;

import java.util.Scanner;

public class input {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
       // System.out.println(input.nextInt()); //(it will input a integer.)
        //System.out.println(input.next()); // it will print anything till it finds the space.
        System.out.println(input.nextLine()); // it will print the entire line, doesn't matter what is in that line.
        
    }
}
